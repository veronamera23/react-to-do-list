specVersion: "v1"
serviceId: "react-to-do-list"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-to-do-list-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
